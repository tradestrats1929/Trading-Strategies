name: Deploy to OCI (dev)

on:
  pull_request:

permissions:
  contents: read
  packages: write

jobs:
  # ── Phase 1: detect which parts of the repo changed ──────────────────────
  changes:
    name: Detect changes
    runs-on: ubuntu-latest
    outputs:
      hello_api:      ${{ steps.filter.outputs.hello_api }}
      txn_cost_api:   ${{ steps.filter.outputs.txn_cost_api }}
      system_monitor: ${{ steps.filter.outputs.system_monitor }}
      angel_api:      ${{ steps.filter.outputs.angel_api }}
      ui:             ${{ steps.filter.outputs.ui }}
      infra:          ${{ steps.filter.outputs.infra }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            hello_api:
              - 'src/libs/hello_lib/**'
              - 'src/libs/service_metrics/**'
              - 'src/services/python/hello_api/**'
              - 'Dockerfile.hello_api'
            txn_cost_api:
              - 'src/libs/txn_cost/**'
              - 'src/libs/service_metrics/**'
              - 'src/services/python/txn_cost_api/**'
              - 'Dockerfile.txn_cost_api'
            system_monitor:
              - 'src/libs/service_metrics/**'
              - 'src/services/python/system_monitor/**'
              - 'Dockerfile.system_monitor'
            angel_api:
              - 'src/libs/service_metrics/**'
              - 'src/services/python/angel_api/**'
              - 'Dockerfile.angel_api'
            ui:
              - 'src/services/typescript/trading-strategies-ui/**'
            infra:
              - 'docker-compose.dev.yml'
              - 'deploy/nginx.conf'

  # ── Phase 2a: build Python service images with :dev tag ──────────────────
  build-hello-api:
    name: Build hello-api image (dev)
    needs: changes
    if: needs.changes.outputs.hello_api == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.hello_api
          push: true
          tags: ghcr.io/tradestrats1929/trading-strategies/hello-api:dev
          build-args: |
            GIT_COMMIT=${{ github.sha }}
            GIT_BRANCH=${{ github.head_ref }}

  build-txn-cost-api:
    name: Build txn-cost-api image (dev)
    needs: changes
    if: needs.changes.outputs.txn_cost_api == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.txn_cost_api
          push: true
          tags: ghcr.io/tradestrats1929/trading-strategies/txn-cost-api:dev
          build-args: |
            GIT_COMMIT=${{ github.sha }}
            GIT_BRANCH=${{ github.head_ref }}

  build-system-monitor:
    name: Build system-monitor image (dev)
    needs: changes
    if: needs.changes.outputs.system_monitor == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.system_monitor
          push: true
          tags: ghcr.io/tradestrats1929/trading-strategies/system-monitor:dev
          build-args: |
            GIT_COMMIT=${{ github.sha }}
            GIT_BRANCH=${{ github.head_ref }}

  build-angel-api:
    name: Build angel-api image (dev)
    needs: changes
    if: needs.changes.outputs.angel_api == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.angel_api
          push: true
          tags: ghcr.io/tradestrats1929/trading-strategies/angel-api:dev
          build-args: |
            GIT_COMMIT=${{ github.sha }}
            GIT_BRANCH=${{ github.head_ref }}

  # ── Phase 2b: build master UI for /dev/ base ─────────────────────────────
  build-ui:
    name: Build master UI (dev)
    needs: changes
    if: needs.changes.outputs.ui == 'true'
    runs-on: ubuntu-latest
    env:
      VITE_HELLO_API_URL: /dev/api/hello
      VITE_TXN_COST_API_URL: /dev/api/txn-cost
      VITE_SYSTEM_MONITOR_API_URL: /dev/api/system-monitor
      VITE_ANGEL_API_URL: /dev/api/angel
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: src/services/typescript/trading-strategies-ui/package-lock.json
      - name: Install deps
        run: npm ci
        working-directory: src/services/typescript/trading-strategies-ui
      - name: Write version file
        run: |
          mkdir -p src/services/typescript/trading-strategies-ui/public
          echo '{"commit":"${{ github.sha }}","branch":"${{ github.head_ref }}"}' \
            > src/services/typescript/trading-strategies-ui/public/version.json
      - name: Build
        run: npm run build -- --base=/dev/
        working-directory: src/services/typescript/trading-strategies-ui
      - uses: actions/upload-artifact@v4
        with:
          name: ui-dist-dev
          path: src/services/typescript/trading-strategies-ui/dist
          retention-days: 1

  # ── Phase 3: deploy to OCI ────────────────────────────────────────────────
  deploy:
    name: Deploy to OCI (dev)
    needs: [changes, build-hello-api, build-txn-cost-api, build-system-monitor, build-angel-api, build-ui]
    if: always() && !cancelled() && !contains(needs.*.result, 'failure')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        if: needs.changes.outputs.ui == 'true'
        with:
          name: ui-dist-dev
          path: ui-dist
        continue-on-error: true

      - uses: appleboy/scp-action@v0.1.7
        with:
          host: 140.245.15.238
          username: ubuntu
          key: ${{ secrets.OCI_SSH_PRIVATE_KEY }}
          source: "docker-compose.dev.yml"
          target: "/opt/trading-strategies/"

      - uses: appleboy/scp-action@v0.1.7
        if: needs.changes.outputs.infra == 'true'
        with:
          host: 140.245.15.238
          username: ubuntu
          key: ${{ secrets.OCI_SSH_PRIVATE_KEY }}
          source: "deploy/nginx.conf"
          target: "/tmp/"
          strip_components: 1

      - uses: appleboy/scp-action@v0.1.7
        if: needs.changes.outputs.ui == 'true'
        with:
          host: 140.245.15.238
          username: ubuntu
          key: ${{ secrets.OCI_SSH_PRIVATE_KEY }}
          source: "ui-dist/"
          target: "/var/www/trading-strategies-ui/dev/"
          strip_components: 1

      - uses: appleboy/ssh-action@v1
        env:
          GHCR_TOKEN:              ${{ secrets.GITHUB_TOKEN }}
          HELLO_API_CHANGED:       ${{ needs.changes.outputs.hello_api }}
          TXN_COST_CHANGED:        ${{ needs.changes.outputs.txn_cost_api }}
          SYSTEM_MONITOR_CHANGED:  ${{ needs.changes.outputs.system_monitor }}
          ANGEL_API_CHANGED:       ${{ needs.changes.outputs.angel_api }}
          INFRA_CHANGED:           ${{ needs.changes.outputs.infra }}
        with:
          host: 140.245.15.238
          username: ubuntu
          key: ${{ secrets.OCI_SSH_PRIVATE_KEY }}
          envs: GHCR_TOKEN,HELLO_API_CHANGED,TXN_COST_CHANGED,SYSTEM_MONITOR_CHANGED,ANGEL_API_CHANGED,INFRA_CHANGED
          script: |
            set -e
            echo "$GHCR_TOKEN" | docker login ghcr.io -u tradestrats1929 --password-stdin

            sudo mkdir -p /var/www/trading-strategies-ui/dev /var/www/trading-strategies-ui/prod
            sudo chown -R ubuntu:ubuntu /var/www/trading-strategies-ui

            cd /opt/trading-strategies

            # Remove any orphaned containers occupying dev ports from prior partial deploys
            for port in 8010 8012 8014 8016; do
              docker ps -q --filter "publish=$port" | xargs -r docker rm -f
            done

            if [[ "$HELLO_API_CHANGED" == "true" ]]; then
              echo "→ Deploying hello_api (dev)"
              docker compose -f docker-compose.dev.yml pull hello_api_dev
              docker compose -f docker-compose.dev.yml up -d --force-recreate --no-deps hello_api_dev
            fi

            if [[ "$TXN_COST_CHANGED" == "true" ]]; then
              echo "→ Deploying txn_cost_api (dev)"
              docker compose -f docker-compose.dev.yml pull txn_cost_api_dev
              docker compose -f docker-compose.dev.yml up -d --force-recreate --no-deps txn_cost_api_dev
            fi

            if [[ "$SYSTEM_MONITOR_CHANGED" == "true" ]]; then
              echo "→ Deploying system_monitor (dev)"
              docker compose -f docker-compose.dev.yml pull system_monitor_dev
              docker compose -f docker-compose.dev.yml up -d --force-recreate --no-deps system_monitor_dev
            fi

            if [[ "$ANGEL_API_CHANGED" == "true" ]]; then
              echo "→ Deploying angel_api (dev)"
              docker compose -f docker-compose.dev.yml pull angel_api_dev
              docker compose -f docker-compose.dev.yml up -d --force-recreate --no-deps angel_api_dev
            fi

            if [[ "$INFRA_CHANGED" == "true" ]]; then
              if [[ -f /tmp/nginx.conf ]]; then
                echo "→ Reloading nginx"
                sudo cp /tmp/nginx.conf /etc/nginx/sites-available/trading-strategies.conf
                sudo ln -sf /etc/nginx/sites-available/trading-strategies.conf /etc/nginx/sites-enabled/trading-strategies.conf
                sudo rm -f /etc/nginx/sites-enabled/default
                sudo nginx -t && sudo systemctl reload nginx
              else
                echo "⚠ /tmp/nginx.conf not found — scp may have failed"
                exit 1
              fi
            fi

            echo "✓ Dev deploy complete"
            docker compose -f docker-compose.dev.yml ps
