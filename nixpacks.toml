[phases.setup]
nixPkgs = ["python312", "uv"]

[phases.install]
cmds = ["uv sync --frozen"]

[start]
cmd = "APP_ENV=production .venv/bin/python -m uvicorn hello_api.main:app --host 0.0.0.0 --port $PORT"
