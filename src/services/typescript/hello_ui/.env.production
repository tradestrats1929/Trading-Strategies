# VITE_API_URL is injected by Railway via the Vercel integration.
# Railway sets it to ${{RAILWAY_PUBLIC_DOMAIN}} which resolves to the
# correct URL per environment (production or PR). Do not hardcode here.
