FastAPI REST service that imports `hello_lib` and exposes its logic over HTTP.

**Production:** https://hello-api-ts.up.railway.app  
**Local:** `uv run uvicorn hello_api.main:app --reload` → http://localhost:8000
